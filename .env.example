# =================================================================
#                Medical Billing Tracker - .env File
# =================================================================
#
# Copy this file to .env and fill in your project's specific values.
# Do not commit the .env file to version control.
#

# -----------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------
# Found in your Supabase project's Settings > Database > Connection string (URI)
DATABASE_URL=postgresql://[user]:[password]@[host]:[port]/[db_name]?sslmode=require

# -----------------------------------------------------------------
# Supabase API Configuration
# -----------------------------------------------------------------
# Found in your Supabase project's Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# IMPORTANT: This is a secret key. Do not expose it on the client side.
# Found in your Supabase project's Settings > API > Project API Keys (service_role secret)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------
# Session & Authentication Configuration
# -----------------------------------------------------------------
# This is a secret key for signing sessions.
# Generate a secure, random string for this value.
# You can use the command `npm run generate:jwt` to create one.
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# -----------------------------------------------------------------
# Server & Environment Configuration
# -----------------------------------------------------------------
# Set to 'development' for local testing, 'production' for deployment
NODE_ENV=development

# The port the backend server will run on
PORT=3000

# The URL of your frontend application (for CORS)
# For local development, this is typically http://localhost:5173
FRONTEND_URL=http://localhost:5173
